<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/gerar_lista_principal"
            android:icon="@drawable/ic_gerar_lista_menu_principal"
            android:title="@string/txt_marcar_visita" />
        <item
            android:id="@+id/lista_principal"
            android:icon="@drawable/ic_lista"
            android:title="@string/txt_lista_projetos" />
        <item
            android:id="@+id/editar_informacoes"
            android:icon="@drawable/ic_edit_info"
            android:title="@string/txt_edit_info" />
    </group>

</menu>
